# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
.vitest/
/report/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# AO data cache (raw CDN dumps, up to ~100 MB)
.cache/

# src/data/ao-data.json is INTENTIONALLY TRACKED, not ignored (see decision-023):
# no CI/deploy step regenerates and ships it, and runtime reads it via fs in
# dynamic server components, so the committed artifact is the only way the
# app gets real data in production.

# local SQLite database file (DATABASE_PATH default)
/data/

# Loop runtime state
.claude/loop/events.jsonl
.claude/loop/last-check.json
.claude/loop/state.json

# ui-reviewer artifacts
.playwright-mcp/
screenshot-*.png
